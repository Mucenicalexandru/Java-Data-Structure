package com.mucenic.hashtable;

public class HashTable {

    //abstract data type
    //doesn't dictate how the data is organized
    //abstract data type is usually an interface - they are not tell you how to store the date
    //they are specifying behaviour

    //for example any class that implements the List interface - is a list

    //HASH TABLES - also known as dictionaries, maps, associative arrays...

    //-provide access using keys
    //-key doesn't have to be an integer
    //-key/value pair doesn't have to be the same type


}
